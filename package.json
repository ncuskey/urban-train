{
  "name": "urban-train",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "prepare": "npx playwright install --with-deps || true",
    "serve": "http-server -p 8000 -c-1 .",
    "test": "playwright test",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "emit:js": "tsc -p tsconfig.emit.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "http-server": "^14.1.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "d3-delaunay": "^6.0.4"
  }
}
